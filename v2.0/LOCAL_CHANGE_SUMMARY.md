# ISO Packer v2.1.0 本地收口摘要

更新时间：2026-07-09

## 边界

- 只改 `D:\Projects\makeiso\v2.0`。
- `D:\Projects\makeiso` 下的 v1.19 只作为只读功能基线。
- VPS 实机测试使用 Docker Hub `ebichu/iso-packer:test`。
- 测试阶段不更新 `latest`。
- 本地安全预览默认不触发真实 CD2 拉取。

## 当前主线状态

- 最新主线提交：本轮 v2.1.0 变更尚未提交，先推 `test` 镜像做 VPS 实测。
- 最新测试镜像：`ebichu/iso-packer:test`
- 本地代码检查通过：Python 编译、前端 JS 语法、`smoke_v2_ok`。
- 本轮新增日志中心、媒体检测页、左侧固定导航和 v2.1.0 版本同步。

## 已完成

### 首页

- 首页已改成展示页方向。
- 已交付原盘资产保留海报墙，桌面一行 5 个。
- 卡片去掉维护状态小字，不显示 `TMDB 海报`、`ISO 归档完成`、`任务名：` 等标签。
- 有 `tmdb_id` 的已交付资产卡片可点击跳转 TMDB。
- 首页状态能区分 CD2 拉取、生成 ISO、校验、转存、CD2 上传云端和本地文件操作。

### 封装中心

- 原“工程体检台 / 封装工作台”命名已收敛为“封装中心”。
- 主状态卡直接显示当前阶段，不再让用户依赖 `SOURCE_PATH` / `OUTPUT_PARTIAL` 判断。
- “产出与转存”面板显示 ISO 生成、校验、转存、CD2 上传和已交付状态。
- `waiting_cd2_upload` 显示为“CD2 上传云端中”。
- 已交付完成态优先显示“完成”，不再被旧 CD2 上传百分比污染。

### 文件浏览

- `cd2` 根目录跟随设置里的网盘挂载根目录，默认 `/CloudNAS/CloudDrive`。
- 目录行可直接点击进入，不再需要单独“进入”按钮。
- 支持多选、右键菜单、重命名、属性面板、目录大小统计、排序、分页和目录选择。
- 属性入口使用图标按钮；属性面板嵌入页面右侧。
- 根切换选中态改成浅灰底，避免黑底过重。
- 文件浏览会记住当前位置，切到其他页面再回来保持原目录。
- “复制到监控目录”走 CD2 网盘内复制任务，目标是设置里的网盘监控目录。
- “复制到输出目录 / 复制到其他目录 / 移动 / 删除”仍为本机文件操作。

### 设置页

- 设置页按“本地目录 / CD2 设置 / 系统设置”三大类组织。
- TMDB 放在系统设置中，并说明它同时服务发行日历和首页已交付资产海报。
- “测试 TMDB”提示已明确：测试不保存，安全保存后才生效。

## VPS 观察结论

- `Wrong Turn 2003`、`Wrong Turn 3 2009` 曾同时进入 CD2 上传队列。
- 这类状态表示 ISO Packer 已写入 CD2 挂载目录，CloudDrive2 正在上传云端。
- 容器 `unhealthy` 的关键原因是大文件转存时 Python 进程卡在 CloudDrive2/FUSE I/O，Web 服务也被堵住。
- `.partial` 文件持续增长时不要强停；强停会清理 partial 并重传。
- 后续必须把封装/转存 worker 与 Web 服务隔离，避免正式版仍出现页面无响应。

## 正式版前检查清单

1. 拉取最新 `ebichu/iso-packer:test` 到 VPS 并重启。
2. 保存线上 TMDB Token，确认已交付原盘资产能补海报。
3. 验证文件浏览“复制到监控目录”进入 CD2 网盘内复制任务，而不是本机复制或远程拉取。
4. 验证封装中心能准确显示 CD2 上传云端、已交付、转存、校验等阶段。
5. 验证本机监控、本机输出空状态不会误导用户；成品以 CD2 挂载输出目录为准。
6. 修复 worker/Web 耦合问题后，再考虑正式推 `latest`。

## 发布策略

- 测试阶段继续只推 `ebichu/iso-packer:test`。
- 正式版 `latest` 需要在 VPS 完成最后一轮实机验收后再推。
- 不要把 `tests/key.txt`、`PROJECT_STAGE_SUMMARY.md`、`v2.0-HTML/` 带入提交。

## 本地保留风险

- 根目录曾有未跟踪 `tests/key.txt`，这是 VPS SSH 私钥，必须留在 Git 外。
- `v2.0-HTML/` 是旧 HTML/导出辅助目录，不属于正式发布。
- `PROJECT_STAGE_SUMMARY.md` 是本地阶段摘要，不属于产品代码。
